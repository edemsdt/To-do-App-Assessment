# Todo-app-assessment

### Prerequisites

You will need mongodb installed on local machine. App uses local mongodb storage.
run "npm install"

### Installing

Run the following
* npm build
* npm start
